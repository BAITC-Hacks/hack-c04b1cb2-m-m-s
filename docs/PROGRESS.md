# Ход работы — 23.09.2026, время Астаны (UTC+5)

Задача: трек «Энергетика», Agentic AI для прогнозирования выработки ВЭС. Основная история проекта — [репозиторий команды](https://github.com/BAITC-Hacks/hack-c04b1cb2-m-m-s).

| Контроль | Фактический результат | Проверка | Публикация |
|---|---|---|---|
| До 14:00 | Скрипт получает архивный прогноз ECMWF IFS HRES 9 км для двух координат на 48 часов, сохраняет время запуска и решения, параметры запроса и кеш | `python3 forecast_weather.py 2026-02-01`: обе турбины по 48 часов; повторный запуск читает кеш; `bad-date` возвращает ошибку | [3d9333c](https://github.com/BAITC-Hacks/hack-c04b1cb2-m-m-s/commit/3d9333ca17949be6b615628c300887153dafdf57), опубликован в 13:36 |

Дополнительная независимая проверка архива: 28 дат с 31.01 по 27.02.2026 × 2 координаты = 56 запросов. Все вернули 48 почасовых значений ветра и температуры без пропусков. Проверка использовала запуск модели предыдущего дня 00:00 UTC и целевое окно 00:00–47:00 UTC. Текущий опубликованный скрипт использует решение в 06:00 UTC и запуск предыдущего дня 18:00 UTC. Оба подхода оставляют запас после типичной задержки публикации глобальной модели 4–6 часов; точное время доступности конкретного запуска пока не доказано метаданными.

После первого checkpoint клиент дополнен скоростью ветра на высоте 100 м и режимом `--days 28`. Локально проверено 56 JSON для 01–28.02.2026: в каждом 48 часов, `wind_speed_100m` без пропусков. Эти изменения пока не опубликованы по просьбе пользователя.

На момент первого checkpoint нет модели выработки, январской проверки точности и автоматического прохода по всему февралю. CSV организатора пока лежат локально вне Git. Официальный репозиторий клонирован после публикации первого checkpoint; дальнейшая разработка идёт в его клоне. Следующий результат: обучение и проверка простой модели на исторических данных, затем соединение с архивной погодой.

После 13:36 пользователь попросил пока не выполнять новые push. Следующие локальные изменения ждут отдельного указания.

Локально после этого: создана модель мощности по двум турбинам, январская оценка без обучения на январе, оркестратор полного цикла и модельные артефакты для запуска без исходных CSV. `python3 run_forecast.py 2026-01-31 --model-dir models --days 29` сформировал 29 файлов, 1 392 почасовые строки (с перекрытием горизонтов), без обращения к личному API-ключу. В первом расчёте 31.01 модель обучена только до 30.01; с 01.02 — до 31.01. Январские результаты и ограничения — [VALIDATION.md](VALIDATION.md). Это пока локальный результат, в remote его нет.

Локальный интерфейс: `python3 web.py` поднимает сервер на `127.0.0.1:8000`. Проверено: главная страница открывается, запрос на 01.02 возвращает 48 строк и график, запрос на 01.03 отклоняется HTTP 400. Визуально проверены заголовок, карточки, график и таблица в браузере. Сервер слушает только localhost, внешний доступ не открывался. Код интерфейса и его проверка также пока не опубликованы.

Во втором часу расширена январская проверка до 31 дня. Кривая, обученная до 31.12.2025, на 733 сопоставленных часах при гипотезе CSV local−UTC = +5 показала MAE 0,1550 и 0,1572 для двух турбин. На 1–14 января отдельно подобран множитель прогнозной скорости ветра 1,10; на отложенных 15–31 января MAE снизилась с 0,1608 до 0,1477 и с 0,1654 до 0,1518. Это не февральская точность; подробности — [VALIDATION.md](VALIDATION.md). Финальная февральская модель содержит параметры поправки и происхождение калибровки.

Сформированы 29 ежедневных JSON в `results/` и сводный `results/february-hourly.csv` на 672 последовательных часа. `python3 export_results.py` проверил покрытие и время принятия решения для каждого выбранного часа. Интерфейс теперь открывает сохранённые результаты без сети; повторный расчёт обращается к погодному архиву и сохраняет новое значение в `predictions/`. API проверен в обоих режимах, они вернули одинаковые 48 часов на 01.02; дата 01.03 отвергнута HTTP 400. В чистой временной папке без `weather-cache/` просмотр 28.02 сработал.

После разрешения пользователя результаты опубликованы в [коммите 1a357a9](https://github.com/BAITC-Hacks/hack-c04b1cb2-m-m-s/commit/1a357a9c7c2c47997e6cc0c77964e4b9bc077f85) до 14:19. `gh api` подтвердил его в `main`. Дополнительно выполнена проверка из настоящего свежего клона: без исходных CSV и погодного кеша сервер открыл сохранённый прогноз на 28.02 (48 часов), а `export_results.py` создал CSV, побайтно совпавший с опубликованным.

После публикации добавлена дополнительная защита входных данных: кеш погоды повторно проверяется на происхождение, координаты, единицы и все 48 последовательных часов; экспорт результатов отвергает пропуск часа, недопустимую мощность, неверное среднее и погодный запуск менее чем за 6 часов до решения. Испорченная копия одного JSON с повторённым часом отклонена с ошибкой. Это опубликовано в [коммите 3a93da5](https://github.com/BAITC-Hacks/hack-c04b1cb2-m-m-s/commit/3a93da5d182b4f09a1273f376ae68f5389d70a39).

Для демонстрации добавлена ссылка на скачивание сводного CSV. Локальный сервер отдал его по HTTP 200 с `text/csv`; `csv.DictReader` прочитал 672 часа. Браузер показал новую ссылку рядом с кнопками расчёта.
